src:
  build:
    target: None
    configuration: None

notests *default:
  build:
    target: Vostok.Commons.Process.sln
    configuration: NoTests
    tool:
      name: dotnet

  install:
    - Vostok.Commons.Process/bin/Release/netstandard2.0/Vostok.Commons.Process.dll

  deps:
    - vostok.logging.abstractions
    - vostok.devtools

full-build > notests:
  build:
    target: Vostok.Commons.Process.sln
    configuration: Release
